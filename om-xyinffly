local Players = game:GetService("Players")
local player = Players.LocalPlayer

while task.wait() do
    pcall(function()
        if player.Character then
            player.Character:AddTag("Kill")
            task.wait()
            player.Character:RemoveTag("Kill")
        end
    end)
end
